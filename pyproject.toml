[project]
name = "bot0-thought-graph"
version = "0.1.0"
description = "Reusable LLM-powered complex-topic disaggregation and thought graphs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.120.0",
    "openai>=2.48.0",
    "pandas>=2.2",
    "pydantic>=2.9",
    "pylint>=4.0.8",
]

[project.optional-dependencies]
providers = [
    "anthropic>=0.39",
    "python-dotenv>=1.0",
    "openai>=1.54",
]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=1.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
include = ["bot0_thought_graph*"]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=1.0",
]

[tool.pytest.ini_options]
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
pythonpath = ["src"]
